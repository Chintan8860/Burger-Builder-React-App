import React from 'react';

import classes from './Spinner.css';

// const spinner = () => (
//     <div className={classes.loader}>Loading...</div>
// );

const spinner = () => (
    <div className={classes.lbs_fb}><div></div><div></div></div>);

export default spinner;